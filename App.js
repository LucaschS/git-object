// const usersAge = [1, 2, 3, 4, 5];

/* const section = document.createElement('section')

usersAge.forEach((age, index, array) => {
    section.innerHTML += (
        `<h1> Użytkownik ${index + 1}</h1>
        <p>wiek: ${age}</p>`
    )
    if (index = array.length - 1) {
        document.body.appendChild(section);
    }
}) */



// array1.forEach(age => total += age)

// console.log(total);

// const myArray = ['apple', 'banana', 'orange'];

// const myList = myArray.map((item) => <li>chuj dupa {item}</li>)

// ReactDOM.render(myList, document.getElementById('root'));

// usersAge.forEach(age => console.log(`chuj kurwa dupa ${age}`));

// console.log(DuzaLitera);

// let total = 0;
// usersAge.forEach(age => total += age);
// console.log(total);

// program to print the text
// declaring a function

// program to add two numbers using a function
// declaring a function
// program to add two numbers
// declaring a function
// program to print a text
// declaring a function


/*
function add(a, b) {
    return a + b;
}

let x = add(2, 4);
console.log(x);
 */


// program to find the square of a number
// function is declared inside the variable

/* 
let x = function (num) { return num * num };
console.log(x(4));

// can be used as variable value for other variables
let y = x(3);
console.log(y);




function fn(a, b, c) {
    return a + b + c;
    return a + b;
}

var x = fn(1, 2, 3);
var y = fn(2, 3, 4);
console.log(x + y); */